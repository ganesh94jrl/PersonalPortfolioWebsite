.hero{text-align:center; padding: 60px 0 80px}
    .hero .tag{margin-bottom:10px}
    .hero h1{font-size:clamp(34px, 6vw, 56px); margin-bottom:12px}
    .hero p{font-size:clamp(16px, 2.2vw, 18px); max-width:65ch; margin-inline:auto}
    .hero-cta{margin-top:18px; display:flex; gap:12px; justify-content:center; flex-wrap:wrap}
    .btn{display:inline-flex; align-items:center; gap:8px; padding:.85rem 1.1rem; border-radius:14px; background:var(--ink); color:var(--bg); text-decoration:none; font-weight:700}
    .btn.secondary{background:transparent; color:var(--ink); border:1.5px solid rgba(255,255,255,.18)}
